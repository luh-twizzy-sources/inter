databaseChangeLog:
  - changeSet:
      id: 002-create-card-info-table
      author: Yafimchyk
      changes:
        - createTable:
            tableName: card_info
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: user_id
                  type: bigint
                  constraints:
                    nullable: false
                    foreignKeyName: fk_card_info_user
                    references: users(id)
                    deleteCascade: true
              - column:
                  name: number
                  type: varchar(255)
                  constraints:
                    nullable: false
              - column:
                  name: holder
                  type: varchar(255)
                  constraints:
                    nullable: false
              - column:
                  name: expiration_date
                  type: date
                  constraints:
                    nullable: false

        - createIndex:
            tableName: card_info
            indexName: idx_card_info_user_id
            columns:
              - column:
                  name: user_id

        - createIndex:
            tableName: card_info
            indexName: idx_card_info_number
            unique: true
            columns:
              - column:
                  name: number

        - createIndex:
            tableName: card_info
            indexName: idx_card_info_expiration_date
            columns:
              - column:
                  name: expiration_date
